  <%= form_with model: task do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.label :details %>
    <%= f.text_field :details %>
    <%= f.check_box :completed unless @new_record%>
    <%= f.label "completed" unless @new_record %>
    <%= f.submit %>
  <% end %>
